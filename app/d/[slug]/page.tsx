import { CrmApp } from '@/components/CrmApp';

// Renders the same SPA shell — client-side code reads the slug from the URL
// and auto-selects the matching department.
export default function DeptPage() {
  return <CrmApp />;
}
